# ---------------------------------------------------------------------------
# MARMORARIA INDEPENDENCIA — variáveis de ambiente
# Copie para .env.local e preencha. NUNCA comite .env.local.
# ---------------------------------------------------------------------------

# Público (pode ir para o browser)
NEXT_PUBLIC_SUPABASE_URL=https://gzkeermtmkpgnxwwxgaq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# URL publica do app. Usada no link de recuperacao de senha.
# Local: http://localhost:3000 | Producao: https://<seu-app>.vercel.app
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Somente local/CI — usado por `npm run db:push` e `npm run db:seed`.
# Session pooler (porta 5432). NÃO vai para a Vercel, NÃO vai para o Git.
SUPABASE_DB_URL=postgresql://postgres.gzkeermtmkpgnxwwxgaq:SENHA@aws-0-sa-east-1.pooler.supabase.com:5432/postgres

# Opcional — somente em ambiente servidor seguro (nunca no client).
# Usado apenas por scripts administrativos de seed de usuários.
SUPABASE_SERVICE_ROLE_KEY=
